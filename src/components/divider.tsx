export default function Divider(){
    return (
        <div style={{
            borderTop: '1px solid #e5e7eb;',  // creates the line
            width: '100%',                  // makes the line span the entire width
            margin: '20px 0'                // adds some space around the line
        }} />
    )
}